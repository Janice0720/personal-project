# K-Means 升級至 K=5 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 將 K-Means 分群從 K=4 升級至 K=5，新增「自雇型」人物類型，並更新所有相關程式、模型、圖表與文件。

**Architecture:** 由內而外逐層更新：核心邏輯層（clustering.py → scoring.py → train_model.py）→ 重新訓練模型（kmeans_model.pkl）→ 圖表重新生成（generate_charts.py）→ App 展示層（streamlit_app.py）→ 文件更新（專案摘要 + 實作計畫）。

**Tech Stack:** Python、scikit-learn、joblib、plotly、kaleido、streamlit、Markdown

---

## 影響範圍總覽

| 類別 | 檔案 | 變更性質 |
|---|---|---|
| 程式 | `scripts/clustering.py` | 修改 CLUSTER_NAMES（4→5 個） |
| 程式 | `scripts/train_model.py` | FINAL_K = 4 → 5 |
| 程式 | `scripts/scoring.py` | 新增自雇型的描述與建議文字 |
| 程式 | `scripts/test_scoring.py` | 新增自雇型的測試案例 |
| 程式 | `scripts/generate_charts.py` | 更新 5 群的排序、顏色與摘要表 |
| 模型 | `models/kmeans_model.pkl` | 重新訓練（K=5） |
| App | `app/streamlit_app.py` | 更新 K=4→K=5 說明與人物表格 |
| 圖表 | `reports/charts/01_elbow_method.png` | 重新標記（標記 K=5 為選定值） |
| 圖表 | `reports/charts/02_silhouette_scores.png` | 反白 K=5（改 K=4 為淺色） |
| 圖表 | `reports/charts/03_cluster_income_structure.png` | 重新生成（5 群） |
| 圖表 | `reports/charts/04_cluster_summary_table.png` | 重新生成（含自雇型） |
| 文件 | `reports/專案摘要_FinCheck個人財務健檢AI系統.md` | 多處更新 K=4→K=5 + 人物表 |
| 文件 | `reports/實作計畫_2026-05-02.md` | 更新 K=4 相關描述與 Self-Review |

---

## Task 1：更新 clustering.py 人物名稱（K=5）

**Files:**
- Modify: `scripts/clustering.py:14-24`

K=5 實際群心特徵（已由分析確認）：
- cluster 0: 薪資 31.8% + 股利 48.0% → **薪資投資型**
- cluster 1: 股利 49.0% + 執行業務 14.5% → **自雇型**
- cluster 2: 股利 49.8% + 租賃 14.7% + 利息 13.4% → **多元資產型**
- cluster 3: 股利 72.4%（高度集中）→ **股利集中型**
- cluster 4: 利息 29.2%（偏高）→ **保守固定型**

- [ ] **Step 1：修改 CLUSTER_NAMES**

```python
# scripts/clustering.py 第 14-24 行，完整替換為：

# cluster 0: 薪資31.8%+股利48.0% → 薪資投資型
# cluster 1: 股利49.0%+執行業務14.5% → 自雇型
# cluster 2: 股利49.8%+租賃14.7%+利息13.4% → 多元資產型
# cluster 3: 股利72.4%（高度集中）→ 股利集中型
# cluster 4: 利息29.2%（偏高）→ 保守固定型
CLUSTER_NAMES = {
    0: '薪資投資型',
    1: '自雇型',
    2: '多元資產型',
    3: '股利集中型',
    4: '保守固定型',
}
```

- [ ] **Step 2：驗證語法正確**

```bash
cd side-projects/personal-fincheck
/opt/anaconda3/bin/python -c "from scripts.clustering import CLUSTER_NAMES; print(CLUSTER_NAMES)"
```

預期輸出：`{0: '薪資投資型', 1: '自雇型', 2: '多元資產型', 3: '股利集中型', 4: '保守固定型'}`

- [ ] **Step 3：Commit**

```bash
git add scripts/clustering.py
git commit -m "feat: K-Means 升級 K=5，新增自雇型人物類別"
```

---

## Task 2：更新 train_model.py

**Files:**
- Modify: `scripts/train_model.py:28-29`

- [ ] **Step 1：修改 FINAL_K**

將第 28-29 行：
```python
# Step 2：以 K=4 訓練（若 best_k 不同仍固定用 4 以確保四種人物類型）
FINAL_K = 4
```
改為：
```python
# Step 2：以 K=5 訓練（三項指標均指向 K=5 為最佳分群數）
FINAL_K = 5
```

- [ ] **Step 2：執行訓練，確認產出正確**

```bash
cd side-projects/personal-fincheck
/opt/anaconda3/bin/python scripts/train_model.py
```

預期輸出：
```
=== 分群結果人數 ===
persona
多元資產型    200
保守固定型    707
自雇型       140
股利集中型   1083
薪資投資型    870
✅ 模型已儲存至 models/kmeans_model.pkl
```

- [ ] **Step 3：Commit**

```bash
git add scripts/train_model.py models/kmeans_model.pkl
git commit -m "feat: 以 K=5 重新訓練 K-Means 模型並更新 pkl"
```

---

## Task 3：更新 scoring.py（新增自雇型描述與建議）

**Files:**
- Modify: `scripts/scoring.py:19-24`（PERSONA_DESCRIPTIONS）
- Modify: `scripts/scoring.py:97-103`（generate_recommendations 的 persona_tips）

- [ ] **Step 1：在 PERSONA_DESCRIPTIONS 新增自雇型**

將 `PERSONA_DESCRIPTIONS` 字典（第 19-24 行）完整替換為：
```python
PERSONA_DESCRIPTIONS = {
    '股利集中型': '你的所得高度依賴股利收入（平均佔 72%），投資組合集中。',
    '薪資投資型': '薪資與股利各佔約三到五成，是兼顧工作與投資的均衡型態。',
    '保守固定型': '利息收入比例偏高，偏好穩定固定收益型資產（定存/債券）。',
    '多元資產型': '股利、租賃、利息三類所得並重，是最多元分散的資產配置型。',
    '自雇型':    '執行業務所得佔約 14.5%，兼具股利收入，屬於自雇或兼業型的複合所得結構。',
}
```

- [ ] **Step 2：在 generate_recommendations 新增自雇型建議**

將 `persona_tips` 字典（第 97-102 行）完整替換為：
```python
persona_tips = {
    '股利集中型': "股利佔比偏高，建議評估個股集中度，若超過 3 成在單一標的，可分散至寬基 ETF 降低個股風險。",
    '薪資投資型': "薪資與股利均衡是良好基礎，建議優先建立 6 個月緊急預備金後，再提高投資配置比例。",
    '保守固定型': "利息所得偏高代表持有較多定存或債券，在升息循環結束後，可評估是否部分轉向股息型資產以提升長期報酬。",
    '多元資產型': "資產配置最為分散，理財觀念成熟，建議定期再平衡（Rebalancing），維持各類資產在目標比例範圍內。",
    '自雇型':    "執行業務所得為浮動性收入，建議強化退休準備（如勞退自提 6%），並考慮保障型保險填補社會保險缺口。",
}
```

- [ ] **Step 3：Commit**

```bash
git add scripts/scoring.py
git commit -m "feat: scoring 模組新增自雇型描述與建議文字"
```

---

## Task 4：更新 test_scoring.py（新增自雇型測試）

**Files:**
- Modify: `scripts/test_scoring.py`（在現有測試末尾新增）

- [ ] **Step 1：新增自雇型測試案例**

在 `test_scoring.py` 現有測試末尾加入：
```python
# 測試：自雇型人物建議包含退休規劃相關文字
tips_ziyong = generate_recommendations('自雇型', diversification_score=50, tax_efficiency_score=60)
assert any('退休' in t or '勞退' in t for t in tips_ziyong), "自雇型建議應包含退休相關文字"
print("自雇型建議測試通過：", tips_ziyong[-1])

# 測試：PERSONA_DESCRIPTIONS 包含全部 5 種人物
from scoring import PERSONA_DESCRIPTIONS
assert len(PERSONA_DESCRIPTIONS) == 5, f"應有 5 種人物描述，實際有 {len(PERSONA_DESCRIPTIONS)} 種"
assert '自雇型' in PERSONA_DESCRIPTIONS, "PERSONA_DESCRIPTIONS 缺少自雇型"
print("人物描述數量驗證通過：", list(PERSONA_DESCRIPTIONS.keys()))
```

- [ ] **Step 2：執行測試確認全部通過**

```bash
cd side-projects/personal-fincheck
/opt/anaconda3/bin/python scripts/test_scoring.py
```

預期：所有 print 輸出正常，無 AssertionError。

- [ ] **Step 3：Commit**

```bash
git add scripts/test_scoring.py
git commit -m "test: 新增自雇型評分與描述測試案例"
```

---

## Task 5：重新生成圖表（generate_charts.py）

**Files:**
- Modify: `scripts/generate_charts.py`（PERSONA_ORDER、COLORS、PERSONA_SUMMARY）
- Regenerate: `reports/charts/01_elbow_method.png`（標記改為 K=5）
- Regenerate: `reports/charts/02_silhouette_scores.png`（反白 K=5）
- Regenerate: `reports/charts/03_cluster_income_structure.png`（5 群）
- Regenerate: `reports/charts/04_cluster_summary_table.png`（含自雇型）

- [ ] **Step 1：修改圖 1（Elbow Method 標記改為 K=5）**

找到 `generate_charts.py` 中 fig1 的 `add_vline`，將 `x=4` 改為 `x=5`，annotation_text 改為 `'選定 K=5'`：
```python
fig1.add_vline(x=5, line_dash='dash', line_color='red',
               annotation_text='選定 K=5', annotation_position='top right')
```

- [ ] **Step 2：修改圖 2（Silhouette 反白 K=5）**

找到 `colors` 的 list comprehension，將條件 `k != 4` 改為 `k != 5`：
```python
colors = ['#A8D8EA' if k != 5 else '#2E86AB' for k in eval_df['k']]
```
同時 `add_vline` 的 `x=4` 改為 `x=5`，annotation_text 改為 `'選定 K=5'`：
```python
fig2.add_vline(x=5, line_dash='dash', line_color='red',
               annotation_text='選定 K=5', annotation_position='top right')
```

- [ ] **Step 3：修改圖 3（5 群人物結構長條圖）**

找到 `COLORS` 清單，新增第 5 色；`persona_order` 更新為 5 個人物：
```python
COLORS = ['#3D9970', '#2E86AB', '#E87722', '#C94040', '#8B5CF6', '#6B7280']
persona_order = ['薪資投資型', '自雇型', '股利集中型', '保守固定型', '多元資產型']
```

同時將訓練由 K=4 改為 K=5：
```python
km = KMeans(n_clusters=5, random_state=42, n_init=10)
```

- [ ] **Step 4：修改圖 4（摘要表加入自雇型）**

找到 `PERSONA_SUMMARY` 字典，新增自雇型：
```python
PERSONA_SUMMARY = {
    '薪資投資型': {
        '特徵': '薪資 ~32% + 股利 ~48%',
        '人數（3000筆）': 870,
        '核心行動（給 B）': '最典型上班族投資人，薪資穩定且持有 ETF/股票',
        '理財建議方向（給 C）': '建立緊急預備金 → 提高投資比例'
    },
    '自雇型': {
        '特徵': '股利 ~49% + 執行業務 ~14.5%',
        '人數（3000筆）': 140,
        '核心行動（給 B）': '兼具業務收入與股利，屬複合所得的自雇或兼業者',
        '理財建議方向（給 C）': '強化退休規劃（勞退自提）+ 保障型保險'
    },
    '股利集中型': {
        '特徵': '股利高達 ~72%',
        '人數（3000筆）': 1083,
        '核心行動（給 B）': '高度依賴股利，可能是退休族或財務自由者',
        '理財建議方向（給 C）': '分散持股至寬基 ETF，降低個股風險'
    },
    '保守固定型': {
        '特徵': '利息 ~29%，低風險偏好',
        '人數（3000筆）': 707,
        '核心行動（給 B）': '偏好定存/債券等固定收益',
        '理財建議方向（給 C）': '升息循環後可部分轉向股息型資產'
    },
    '多元資產型': {
        '特徵': '股利 ~50% + 租賃 ~15% + 利息 ~13%',
        '人數（3000筆）': 200,
        '核心行動（給 B）': '股利/租賃/利息三類並重，最多元',
        '理財建議方向（給 C）': '定期再平衡（Rebalancing），維持目標比例'
    },
}
```

- [ ] **Step 5：執行生成腳本**

```bash
cd side-projects/personal-fincheck
/opt/anaconda3/bin/python scripts/generate_charts.py
```

預期輸出：
```
生成圖 1：Elbow Method ...
生成圖 2：Silhouette Score ...
生成圖 3：各群體所得結構長條圖 ...
生成圖 4：分群摘要表 ...
✅ 全部圖表已儲存至 reports/charts/
```

- [ ] **Step 6：Commit**

```bash
git add scripts/generate_charts.py reports/charts/
git commit -m "feat: 重新生成 K=5 分群圖表（含自雇型）"
```

---

## Task 6：更新 streamlit_app.py

**Files:**
- Modify: `app/streamlit_app.py:114-126, 142`

- [ ] **Step 1：更新頁首 Caption**

第 142 行，`K-Means 分群（K=4）` 改為 `K-Means 分群（K=5）`：
```python
    "**財政部 111 年統計** × **K-Means 分群（K=5）**｜"
```

- [ ] **Step 2：更新 AI 分群說明 Expander**

第 114-126 行的 expander 內容完整替換為：
```python
    with st.expander("🤖 AI 分群說明（K-Means 模型）"):
        st.markdown("""
        本系統使用 **K-Means 分群（K=5）** 分析 3,000 筆合成個體資料（基於財政部 111 年度統計），
        依所得結構特徵分為五種財務人物類型：

        | 人物類型 | 特徵描述 |
        |---|---|
        | 薪資投資型 | 薪資 ~32% 搭配股利 ~48%，最典型的上班族投資人 |
        | 自雇型 | 執行業務所得 ~14.5% 搭配股利，屬自雇或兼業複合所得結構 |
        | 股利集中型 | 股利所得佔比 > 70%，高度集中於股票/ETF |
        | 保守固定型 | 利息所得佔比 > 25%，偏好定存/債券等固定收益 |
        | 多元資產型 | 股利、租賃、利息三類並重，資產最為分散 |

        **模型評估（K=2～8 全面比較）**：
        - Silhouette Score（K=5）= **0.310**（所有 K 值最高）
        - Davies-Bouldin（K=5）= **1.048**（所有 K 值最低，越低越好）
        - Calinski-Harabasz（K=5）= **1088**（所有 K 值最高）
        - 分群穩定性：10 個不同初始值均收斂至相同結果（std = 0.0001）
        """)
```

- [ ] **Step 3：本地確認 Streamlit 可正常啟動**

```bash
cd side-projects/personal-fincheck/app
/opt/anaconda3/bin/streamlit run streamlit_app.py
# 瀏覽 http://localhost:8501，確認 AI 分群說明顯示 5 種人物類型
```

- [ ] **Step 4：Commit**

```bash
git add app/streamlit_app.py
git commit -m "feat: Streamlit App 更新為 K=5 分群說明與五種人物類型"
```

---

## Task 7：更新專案摘要（專案摘要_FinCheck個人財務健檢AI系統.md）

**Files:**
- Modify: `reports/專案摘要_FinCheck個人財務健檢AI系統.md`

需更新的位置共 7 處：

- [ ] **Step 1：訓練架構說明（第 83 行）**

`K-Means 分群訓練（預計 K=4）` → `K-Means 分群訓練（K=5，三項指標均最優）`

- [ ] **Step 2：模組三 Elbow 說明（第 175 行）**

`選取「手肘轉折點」作為最佳 K 值（預計為 K=4）`
→ `選取「手肘轉折點」作為收斂起點，並以 Silhouette Score、Davies-Bouldin、Calinski-Harabasz 三項指標共同確認最佳 K 值為 K=5。`

- [ ] **Step 3：模組三分群結果表（第 183-202 行）**

`預計四種財務人物類型` → `五種財務人物類型`，人物表格替換為：

```markdown
| 人物類型 | 特徵 | 建議方向 |
|---|---|---|
| 薪資投資型 | 薪資 ~32%、股利 ~48% | 建立緊急預備金、逐步提高投資比例 |
| 自雇型 | 執行業務 ~14.5%、股利 ~49% | 強化退休規劃（勞退自提）、配置保障型保險 |
| 股利集中型 | 股利佔比 > 70% | 資產配置多元化、分散至寬基 ETF |
| 保守固定型 | 利息佔比 > 25% | 升息後評估轉向股息型資產 |
| 多元資產型 | 股利/租賃/利息三類並重 | 定期再平衡、維持目標比例 |
```

- [ ] **Step 4：成員 B 工作說明（人物描述表，第 299-307 行）**

`四種人物類型` → `五種人物類型`，表格新增自雇型一行：

```markdown
| 自雇型 | 業務所得 ~14.5% + 股利 ~49% | 有穩定業務收入又兼顧股利的複合型，可能是自由工作者、顧問或兼職業主 |
```

- [ ] **Step 5：成員 C 工作說明（建議方向表，第 352-357 行）**

表格新增自雇型一行：

```markdown
| 自雇型 | 業務收入浮動，退休保障不足 | 建議勞退自提 6%、配置保障型保險，形成安全網後再提高投資比例 |
```

- [ ] **Step 6：Streamlit 示意圖（第 222 行）**

`你的財務人物類型：投資型` → `你的財務人物類型：薪資投資型`

- [ ] **Step 7：Commit**

```bash
git add reports/專案摘要_FinCheck個人財務健檢AI系統.md
git commit -m "docs: 專案摘要更新為 K=5，新增自雇型人物類別"
```

---

## Task 8：更新實作計畫（實作計畫_2026-05-02.md）

**Files:**
- Modify: `reports/實作計畫_2026-05-02.md`

- [ ] **Step 1：更新 Task 4 Step 2 的預期輸出說明（第 363 行）**

`K=4 的 Silhouette Score 應在 0.25～0.50 之間`
→ `K=5 的 Silhouette Score 為最高值（0.310）；K=4 為 0.290`

- [ ] **Step 2：更新 Task 4 Step 3 標題（第 365 行）**

`訓練最終模型（K=4）` → `訓練最終模型（K=5）`

- [ ] **Step 3：更新 Task 4 Step 5 的 commit message（第 446 行）**

`K=4，模型存檔` → `K=5，三項指標最優，模型存檔`

- [ ] **Step 4：更新 Self-Review Checklist（第 748、751 行）**

```markdown
- [x] **B AI/ML 建模**：Task 4（K-Means K=5 + Elbow + Silhouette + Davies-Bouldin + Calinski-H）+ Task 5（評分）涵蓋
- [x] **模型評估指標**：三項指標（Silhouette、Davies-Bouldin、Calinski-Harabasz）均在 Task 4 完整執行，K=5 全面最優
```

- [ ] **Step 5：Commit**

```bash
git add reports/實作計畫_2026-05-02.md
git commit -m "docs: 實作計畫更新 K=4→K=5 相關說明"
```

---

## Self-Review Checklist

- [ ] **Task 1～2 順序正確**：clustering.py 的 CLUSTER_NAMES 必須在 train_model.py 執行前更新，否則 pkl 會存入錯誤的 names
- [ ] **Task 2 與 Task 5 連動**：generate_charts.py 裡 `km = KMeans(n_clusters=5)` 與 `PERSONA_SUMMARY` 的人數（870/140/200/1083/707）須與 train_model.py 產出一致
- [ ] **Task 3 的人物名稱**：scoring.py 的 key（`'自雇型'`）必須與 clustering.py 的 CLUSTER_NAMES 值完全一致，否則 predict_persona 回傳值會對應不到建議文字
- [ ] **Task 4 測試**：test_scoring.py 執行後無 AssertionError
- [ ] **Task 6 App**：Streamlit 上傳真實 PDF（個人資料所得_20260502.pdf）後，確認人物類型顯示為「薪資投資型」（薪資 88% 的特徵不會落入自雇型）
- [ ] **圖表人數與訓練結果一致**：generate_charts.py 的 PERSONA_SUMMARY 人數為硬編碼，需與 train_model.py 的輸出對齊（870/140/200/1083/707）
